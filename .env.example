# ShipLog — Environment Variables
# Copy to .env.local and fill in your values

# GitHub OAuth App
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# OpenAI (for PR categorization)
OPENAI_API_KEY=

# App URL (used for OAuth callbacks and public links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Session encryption secret (generate with: openssl rand -hex 32)
SESSION_SECRET=

# Database — Turso (production) or local SQLite file (development)
# For local dev: file:data/shiplog.db (default if unset)
# For Turso: libsql://your-db-name-your-org.turso.io
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
